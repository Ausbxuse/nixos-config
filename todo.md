1. Set up nix-secrets with sops-nix — SSH-derived age keys, API keys, TOTP seeds, app passwords, public/private
   split
2. Set up Syncthing mesh — full mesh (phone ↔ razy ↔ timy ↔ VPS), declarative folders
3. Set up auto-backup to external drive — udev + systemd auto-rsync on plug-in
4. Windows recovery USB + update BIOS — for razy
5. Reproducible phone setup — Aegis (TOTP), KeePassXC, Signal backup, Syncthing, document recovery steps
6. documentations for emergency cases
7. secure boot

# razy specific issues

1. no auto brightness
2. does not dim before turn off screen

# host

1. rounded corners not applied in new host
2. "provisioin", "ad hoc" wording replacement
